# Core app dependencies (Streamlit Cloud installs from this file)
streamlit==1.58.0
pandas==3.0.3
plotly==6.8.0
scikit-learn==1.9.0
joblib==1.5.3

# Data download helpers (not needed by the deployed app, harmless to include)
requests==2.34.2
tqdm

# Notebooks / matplotlib for local analysis
matplotlib==3.11.0
